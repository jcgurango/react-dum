{
  "name": "react-dum",
  "version": "0.0.4",
  "description": "React package for rendering to a plain JS object, with a callback whenever the tree changes.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react-reconciler": "^0.25.1"
  }
}
